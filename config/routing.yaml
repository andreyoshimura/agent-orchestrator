routing:
  explain-file:
    preferred: claude
    fallback: [gemini, openai]
    execution:
      max_provider_retries: 0
      provider_timeout_sec: 30
      fallback_on: [temporary, rate_limit, network, configuration, provider_unavailable]
  review-file:
    preferred: claude
    fallback: [gemini, openai]
    execution:
      max_provider_retries: 1
      provider_timeout_sec: 30
      fallback_on: [temporary, rate_limit, network, configuration, provider_unavailable]
  review-snippet:
    preferred: claude
    fallback: [gemini, openai]
    execution:
      max_provider_retries: 1
      fallback_on: [temporary, rate_limit, network, configuration, provider_unavailable]
  review-diff:
    preferred: gemini
    fallback: [claude, openai]
    execution:
      max_provider_retries: 1
      fallback_on: [temporary, rate_limit, network, configuration, provider_unavailable]
  map-dependencies:
    preferred: gemini
    fallback: [claude, openai]
    execution:
      max_provider_retries: 1
      provider_timeout_sec: 45
      fallback_on: [temporary, rate_limit, network, configuration, provider_unavailable]
  summarize-module:
    preferred: gemini
    fallback: [claude, openai]
    execution:
      max_provider_retries: 1
      fallback_on: [temporary, rate_limit, network, configuration, provider_unavailable]
  compare-options:
    preferred: openai
    fallback: [gemini]
    execution:
      max_provider_retries: 1
      fallback_on: [temporary, rate_limit, network, configuration, provider_unavailable]
  final-decision:
    preferred: openai
    fallback: [gemini]
    execution:
      max_provider_retries: 1
      fallback_on: [temporary, rate_limit, network, configuration, provider_unavailable]
